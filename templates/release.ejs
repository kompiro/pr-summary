## Contributors

<%
prInfo.contributors.forEach(function(contributor) { -%>
- [ ] @<%= contributor %>
<% }); -%>

## Merged Pull Requests

<% prInfo.prs.forEach(function(pr) {
  var users = pr.participants.nodes.map((user) => '@' + user.login).join(', ')
-%>
- #<%= pr.number %> <%= pr.title %> by <%= users %>
<% }); -%>
